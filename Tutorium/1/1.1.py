temp = int(input("Bitte eine Zahl eingeben: "))
divisor = int(input("Bitte eine weitere Zahl eingeben: "))

dividend = temp + divisor

print(dividend)

ergebnis = dividend / divisor
print(ergebnis)

rest = dividend % divisor
print(rest)